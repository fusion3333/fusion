# Taiwan NCCC card public panel — evidence-supported observation

Date: 2026-08-28

Official Taiwan/NCCC public datasets verify 2014+ monthly foreign-card Taiwan spend by industry/region and Taiwan-card overseas top-destination spend. These are defined card-processing transaction panels, not total tourism expenditure.

Guardrails: foreign-card spend != foreign-tourist total spend; cardholder != nationality; transaction region != travel itinerary; transaction count != traveler count; industry buckets are broader than tourism categories.

Current pipeline: `source discovered → official dataset IDs/schema/license verified → endpoint registered → bulk row ingestion pending`.